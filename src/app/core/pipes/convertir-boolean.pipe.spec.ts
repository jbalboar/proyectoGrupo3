import { ConvertirBooleanPipe } from './convertir-boolean.pipe';

describe('ConvertirBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
